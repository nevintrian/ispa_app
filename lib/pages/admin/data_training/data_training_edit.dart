import 'package:flutter/material.dart';

class DataTrainingEdit extends StatelessWidget {
  const DataTrainingEdit({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
